import './register.css';

const Register = () => {

    const handleClick = () => {
        // register backend
    }

    return (
        <div className="register">Register</div>
    );
};

export default Register;
